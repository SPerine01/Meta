    <header class="intro-header header2">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                    <div class="post-heading">
                        <h1> <%= @post.title %> </h1>
                        <span class="meta"><%= link_to "Edit", edit_post_path(@post) %> | <%= link_to "Delete", post_path(@post), method: :delete, data: { confirm: "Are you sure?" } %> </span>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <article>
    <div class="container">
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">

                		<%= @post.body %>

                		<% if @post.comments.count > 0 %>
							<h3>All Comments</h3>
	                		<hr>

	                		<%= render @post.comments.order("created_at DESC") %>
	                		<% end %>
                </div>
            </div>
    </div>
    </article>

		<div class="blog_posts">

			<p>
				<span class="meta"><%= link_to "Back", posts_path %> </span>

			</p>
		</div>

	<div class="comments">

		<div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                <h4>Leave a Comment</h4>
			<%= form_for [@post, @post.comments.build] do |f| %>
                    <div class="row control-group">
                        <div class="form-group col-xs-12 floating-label-form-group controls">
                          <%= f.label :name %> 
                            <%= f.text_field :name, class: "form-control", placeholder: "Name" %>
                           <p class="help-block text-danger"></p>
                        </div>
                    </div>
                  
                   
                    <div class="row control-group">
                        <div class="form-group col-xs-12 floating-label-form-group controls">
                        	<%= f.label :body %> 
                            <%= f.text_area :body, class: "form-control", placeholder: "Comment" %>
                            <p class="help-block text-danger"></p>
                        </div>
                    </div>
                    <br>
                    <div id="success"></div>
                    <div class="row">
                        <div class="form-group col-xs-12">
                            <%= f.submit class: "btn btn-default" %>
                        </div>
                    </div>
                <% end %>
            </div>
        </div>
    </div>